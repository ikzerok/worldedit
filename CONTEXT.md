# worldedit

桌面/Web 作者工作台：地图画布、资料侧栏、关系网络与故事视图的交互层；内容真源在 worldline-core。

## Language

**共享快照**:
一次编译结果（Snapshot）驱动的只读视图数据源；结构化修改走命令而非直接改快照。
_Avoid_: 全局状态

**镜头**:
pan/zoom 构成的个人浏览状态，不入工程；与共享展示配置（visible_default、布局）相对。
_Avoid_: 视图、视角

**编辑展示模式**:
显式进入后才可创建/拖动图元的模式；浏览模式下一切交互零写入。
_Avoid_: 编辑模式（省略"展示"会与内容编辑混淆）

**检查器**:
右侧统一的资料/标记信息面板；同名对象以 kind+ID 后缀消歧。
_Avoid_: 侧栏（口语化）、属性面板

**栅格图层**:
PNG/JPEG 位图放置进地图坐标系的展示图层，带放置矩形；不是底图。见组合目录 ADR-0002。
_Avoid_: 底图、背景图

**一次拖动一次撤销**:
pointer_move 仅预览，pointer_up 提交一个命令，Undo 一步恢复；取消不成历史。
_Avoid_: 拖拽事务

## 当前开发切片

WP-10 已接入通用实体、独立关系和关系类型表单及删除确认，但网络画布尚未实现，工单未关闭。
实现边界、验证结果及剩余项见 `docs/wp10-progress.md`，不要将部分交付当作 M2 全验收。
