# 发布验证记录（2026-09-08）

## 自动验证

- worldline：151 项测试通过，包含运行、语言、CLI、RPC、目录边界、完整导出与文档测试。
- worldedit：6 项测试通过，包含 ZIP 往返、路径检查与共享导出编译。
- 两个独立仓库格式检查、严格 Clippy；编辑器 wasm32 严格 Clippy 通过。
- worldline 公开 Rust API 文档生成成功，源码手册链接扫描无失效相对链接。
- worldedit-authoring 技能通过官方 quick_validate 格式检查。
- Windows 与 Web release 构建成功；源码 ZIP 排除 target/.git，Windows 与编辑器源码包包含隐藏 .agent/skills。

## 桌面实际操作

用发布版打开隔离雾港工作区，通过启动目录选择框选定根目录。磁盘新增 recursive/deep/discovered.wl（没有 include）后，界面源码数量由 4 变为 5；人物页出现 workspace_probe / 自动刷新测试人物，证明递归发现和对象索引刷新接入实际 UI。

随后用编辑器“导出工程”导出到工作区外新目录。原目录与导出目录的 6 个文件逐字节一致，含未引用深层源码与 note.md。对导出目录运行 wl check，ok:true 且 diagnostics 为空。

## 子时段与分支编辑补充验证

新增 4 项核心回归覆盖多层包含、非法上级/自包含/循环、编辑后的指纹稳定性与事务回滚，以及选择新增、删除、嵌套结构和注释保留、运行时选项执行。

用 Windows release 打开隔离工作区，确认“全年 → 九月 → 九月八日”嵌套展示。在“全年”点击“＋ 子时段”，表单自动选择 year，保存后源码含 `within year`。打开事件，修改选项文案并分两次输入两行正文（中间停顿），顶部“应用更改”后“保存全部”，磁盘文件内容一致，wl check 返回 ok:true。随后通过“＋ 添加选项”新增默认结束分支并保存。缩小时间线时按钮文字随缩放调整。

## 标签操作补充验证

新增条件组合回归验证已有 or 条件与多个 not has 条件组合时保持括号优先级，且生成文本通过核心编译。6 项编辑器测试、原生与 wasm32 严格检查通过。标签搜索、多选、创建并绑定、标签关联、条件与动作控件已编译；实际界面验证由用户中止，未完成新增标签到保存的完整人工回归。

## 验证边界

本次 UI 冒烟覆盖目录选择、递归新增、人物索引与完整导出；未对每个表单、每个图操作做完整人工回归。新增旧表单防覆盖保护通过编译和严格检查，尚未做专门 UI 冲突演练。Web 已构建和静态检查，未把浏览器快照宣称为实时磁盘同步。无 GitHub 上传、签名或安装器验证。
